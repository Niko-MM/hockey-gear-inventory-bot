name: hockey-bot

services:
  bot:
    build: .
    env_file: .env
    environment:
      DB_PATH: /app/data/bot.db
    volumes:
      - bot-data:/app/data
    restart: unless-stopped

volumes:
  bot-data:
